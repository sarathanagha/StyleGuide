/// <reference path="../references.ts" />

module Microsoft.DataStudio.Diagnostics.Logging {

    export enum LogLevel {
        Debug = 10,
        Info = 20,
        Warning = 30,
        Error = 40
    }
}
